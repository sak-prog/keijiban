<h1>掲示板</h1>
<%= link_to new_board_path do %>スレッド新規作成<% end %>
<hr>
<% @boards.each do |board| %>
  <%= link_to board.title, board_path(board) %>
  <small class="text-muted"><%= board.updated_at.strftime("%Y-%m-%d %H:%M") %></small>
  </br>
<% end %>